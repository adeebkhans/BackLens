{
  "name": "backendlens",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "bootstrap": "pnpm install",
    "dev:parser": "pnpm --filter parser dev",
    "build": "pnpm -w -s -r run build"
  },
  "devDependencies": {
    "@types/babel__parser": "^7.1.5",
    "pnpm": "10.24.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3"]
  }
}